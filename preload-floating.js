// preload-floating.js
// No specific preload logic needed for this simple indicator window,
// but keeping the file structure consistent.

// Optionally, you could add contextBridge expose if needed later,
// but for just displaying HTML/CSS, it's not required. 